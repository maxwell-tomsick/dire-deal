#include "JsonLoader.h"
#include <cugl/cugl.h>
using namespace cugl;
namespace JsonLoader{

   // static std::shared_ptr<JsonValue> getJsonItem(string key, string type) {
        //std::shared_ptr<JsonReader> jsonReader = JsonReader::alloc(key);
        //return jsonReader->readJson()->get(type);
   // }

}